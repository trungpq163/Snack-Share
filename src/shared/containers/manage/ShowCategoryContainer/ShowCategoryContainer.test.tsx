import * as React from 'react';
import { shallow } from 'enzyme';

import ShowCategoryContainer from './ShowCategoryContainer';

describe('ShowCategoryContainer', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<ShowCategoryContainer {...defaultProps} />);
    });
});
