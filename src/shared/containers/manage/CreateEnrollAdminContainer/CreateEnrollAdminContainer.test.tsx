import * as React from 'react';
import { shallow } from 'enzyme';

import CreateEnrollAdminContainer from './CreateEnrollAdminContainer';

describe('CreateEnrollAdminContainer', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<CreateEnrollAdminContainer {...defaultProps} />);
    });
});
