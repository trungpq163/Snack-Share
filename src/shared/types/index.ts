export interface User {
    // eslint-disable-next-line camelcase
    first_name?: string;
    // eslint-disable-next-line camelcase
    last_name?: string;
    email?: string;
    password?: string;
    password2?: string;
    role?: string;
}
