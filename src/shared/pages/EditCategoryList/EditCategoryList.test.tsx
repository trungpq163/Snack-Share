import * as React from 'react';
import { shallow } from 'enzyme';

import EditCategoryList from './EditCategoryList';

describe('EditCategoryList', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<EditCategoryList {...defaultProps} />);
    });
});
