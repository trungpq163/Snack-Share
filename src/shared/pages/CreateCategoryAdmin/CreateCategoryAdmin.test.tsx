import * as React from 'react';
import { shallow } from 'enzyme';

import CreateCategoryAdmin from './CreateCategoryAdmin';

describe('CreateCategoryAdmin', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<CreateCategoryAdmin {...defaultProps} />);
    });
});
