import * as React from 'react';
import { shallow } from 'enzyme';

import ShowCategoryList from './ShowCategoryList';

describe('ShowCategoryList', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<ShowCategoryList {...defaultProps} />);
    });
});
