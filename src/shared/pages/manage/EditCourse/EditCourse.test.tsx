import * as React from 'react';
import { shallow } from 'enzyme';

import EditCourse from './EditCourse';

describe('EditCourse', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<EditCourse {...defaultProps} />);
    });
});
