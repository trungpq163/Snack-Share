import * as React from 'react';
import { shallow } from 'enzyme';

import CreateEnrollAdmin from './CreateEnrollAdmin';

describe('CreateEnrollAdmin', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<CreateEnrollAdmin {...defaultProps} />);
    });
});
