import * as React from 'react';
import { shallow } from 'enzyme';

import ShowCategory from './ShowCategory';

describe('ShowCategory', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<ShowCategory {...defaultProps} />);
    });
});
