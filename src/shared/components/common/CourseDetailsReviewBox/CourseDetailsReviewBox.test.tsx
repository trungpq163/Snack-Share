import * as React from 'react';
import { shallow } from 'enzyme';

import CourseDetailsReviewBox from './CourseDetailsReviewBox';

describe('CourseDetailsReviewBox', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<CourseDetailsReviewBox {...defaultProps} />);
    });
});
