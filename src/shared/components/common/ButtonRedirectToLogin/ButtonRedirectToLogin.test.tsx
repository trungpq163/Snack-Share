import * as React from 'react';
import { shallow } from 'enzyme';

import ButtonRedirectToLogin from './ButtonRedirectToLogin';

describe('ButtonRedirectToLogin', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<ButtonRedirectToLogin {...defaultProps} />);
    });
});
