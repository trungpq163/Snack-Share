import * as React from 'react';
import { shallow } from 'enzyme';

import CourseDetailsComment from './CourseDetailsComment';

describe('CourseDetailsComment', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<CourseDetailsComment {...defaultProps} />);
    });
});
