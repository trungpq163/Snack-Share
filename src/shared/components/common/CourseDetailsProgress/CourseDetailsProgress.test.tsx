import * as React from 'react';
import { shallow } from 'enzyme';

import CourseDetailsProgress from './CourseDetailsProgress';

describe('CourseDetailsProgress', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<CourseDetailsProgress {...defaultProps} />);
    });
});
