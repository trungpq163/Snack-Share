import * as React from 'react';
import { shallow } from 'enzyme';

import VerticalBarChart from './VerticalBarChart';

describe('VerticalBarChart', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<VerticalBarChart {...defaultProps} />);
    });
});
